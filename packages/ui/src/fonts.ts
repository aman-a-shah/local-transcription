// Self-hosted fonts (no third-party CDN — the privacy principle). Import this
// once from each app's entry point. Variable fonts keep the payload small.
import "@fontsource-variable/schibsted-grotesk";
import "@fontsource-variable/jetbrains-mono";
